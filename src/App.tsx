import { FC } from 'react';
import BoilerplateInfo from './pages/bolierPlateInfo/BoilerPlateInfo';

interface AppProps {}

const App: FC<AppProps> = () => {
  return <BoilerplateInfo />;
};

export default App;
